### What is a Private IP Address? (Interview)

**Answer:**

A **Private IP Address** is an IP address used **within a local network (LAN)**. It is assigned by a **router** and **cannot be accessed directly from the internet**.
