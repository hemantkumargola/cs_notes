What is a Peer-to-Peer (P2P) Network?

A Peer-to-Peer (P2P) Network is a network in which all computers have equal roles and can share resources directly with each other without using a central server.

Example: File sharing between two computers on the same network.