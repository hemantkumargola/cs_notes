What is a Default Gateway? (Interview Answer - Short)

Definition:
A default gateway is a router that forwards data from a local network to other networks, such as the Internet.

One-line Answer:

A default gateway is the router that connects a local network to other networks (like the Internet).